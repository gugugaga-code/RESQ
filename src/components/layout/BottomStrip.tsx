import { Command, Layers3, SlidersHorizontal } from 'lucide-react'
import { SystemStatusBar } from '../status/SystemStatusBar'
import { useCrisisStore } from '../../store/useCrisisStore'
export function BottomStrip() { const status = useCrisisStore((state) => state.simulationStatus); const step = useCrisisStore((state) => state.simulationStep); const event = useCrisisStore((state) => state.simulationEvent); const start = useCrisisStore((state) => state.startSimulation); const pause = useCrisisStore((state) => state.pauseSimulation); const reset = useCrisisStore((state) => state.resetSimulation); return <footer className="bottom-strip"><SystemStatusBar /><div className="simulation-control"><strong>SIMULATION</strong><span>{status} · {step}/5</span><small>{event ?? 'Major Flood Response ready'}</small><button onClick={start}>{status === 'running' ? 'RUNNING' : 'START'}</button><button onClick={pause}>PAUSE</button><button onClick={reset}>RESET</button></div><div className="control-placeholder"><span><Layers3 size={14} /> Layers</span><span><SlidersHorizontal size={14} /> Filters</span><span><Command size={14} /> Command palette</span></div></footer> }
