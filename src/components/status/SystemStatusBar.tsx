import { Radio, Server, Users } from 'lucide-react'
import { useCrisisStore } from '../../store/useCrisisStore'
export function SystemStatusBar() { const system = useCrisisStore((state) => state.system); const lastSynced = new Intl.DateTimeFormat('en-IN', { hour: '2-digit', minute: '2-digit', hour12: false }).format(new Date(system.lastSyncedAt)); return <div className="system-status"><span><Radio size={14} /> {system.activeIncidentCount} active incidents</span><span><Users size={14} /> {system.activeResourceCount} units engaged</span><span><Server size={14} /> {system.syncStatus === 'syncing' ? 'Local sync in progress' : `Synced ${lastSynced} IST`}</span><span>Pending sync: {system.pendingSyncActions}</span></div> }
